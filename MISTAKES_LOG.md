# GrayLight Studio 开发错题集 (Mistakes Log)

## ❌ 案例 001：图层排序 UI 刷新失效

- **发生时间**：2026-05-11
- **现象**：图层拖拽排序后，内存中的底图已自动激活可见，但 UI 上的“眼睛”图标依然显示为灰色关闭状态。
- **根因分析**：
    1. **选择器命名脱节 (Main Reason)**：在 `syncLayers` 局部更新函数中，使用了 `.visible-btn` 作为选择器，但 HTML 实际定义的类名为 `.eye-btn`。导致 `querySelector` 始终返回 `null`，更新逻辑被静默跳过。
    2. **映射逻辑混乱**：在计算 DOM 节点与数组对象的对应关系时，未能正确处理“反向渲染（底图在下）”与“DOM 树序（底图在后）”的抵消关系，导致 index 计算反复出现正反倒置。
- **解决方案**：
    1. 统一选择器为 `.eye-btn svg`。
    2. 明确映射公式：`items[index]` (从上到下) ➔ `layers[length - 1 - index]` (从顶到底)。
    3. 废弃 `WeakMap` 之外的脆弱索引依赖。
- **教训**：在进行局部 DOM 更新时，必须先验证选择器的真实性，严禁凭记忆编写 classname。

## ❌ 案例 004：修复 UI Bug 时波及核心渲染协议 (Regression)

- **发生时间**：2026-05-13
- **现象**：在修复“缩略图消失”和“重置闪烁”等 UI 细节时，WebGL 的光效层级逻辑出现混乱，导致多图层模式下光效熄灭或叠加错误。
- **根因分析**：在 `renderCanvas` 循环中错误地修改了 `u_bright` 的分配逻辑，未能准确区分 `u_bright` 在 Shader 中对“光效强度”与“底图可见度”的复合控制作用。
- **解决方案**：
    1. 严格锁定渲染协议：单图 0.8；多图底图 0.0（留底色），上方层 0.8（留光效）。
    2. 执行 UI 修改后，必须同步验证 WebGL 渲染状态的正确性。
- **教训**：严禁在未彻底读懂 Shader 源码变量含义的情况下修改渲染循环逻辑。UI 修复必须与核心渲染引擎逻辑进行物理隔离测试。

## ❌ 案例 002：CSS Zoom 导致的布局裁剪 (Layout Clipping)

- **发生时间**：2026-05-13
- **现象**：浏览器窗口在常规尺寸下显示不全，底部控制面板或右上角按钮被物理裁掉，且无法通过滚动找回。
- **根因分析**：使用了 `zoom: 1.1` 进行全局放大。`zoom` 属性虽然能简单实现缩放，但在某些渲染引擎中会导致容器的 `getBoundingClientRect` 计算与视觉表现不一致，从而引发严重的 Overflow 溢出错误，将 UI 挤出视口。
- **解决方案**：撤销 `zoom`，改为“结构化缩放（Structural Scaling）”——通过手动增加宽度 (`--sidebar-w`)、字体大小 (`text-[11px]`) 和内边距 (`p-7`) 来实现 110% 的放大效果。
- **教训**：在工业级 UI 开发中，严禁依赖 `zoom` 这种“捷径”属性进行缩放，必须通过调整布局变量实现物理级别的尺寸增长。

## ❌ 案例 003：大规模代码重构导致的逻辑“丢失”

- **发生时间**：2026-05-13
- **现象**：在微调 UI 布局后，原本正常的 WebGL 动画停止了，或者之前写好的拖拽功能莫名消失。
- **根因分析**：在执行大规模代码重写或全量替换时，未能对包含业务逻辑的脚本块进行“原子级”保护。在合并代码段时，不小心误删了 `pAnim` 声明或破坏了 `isPlaying` 时钟链路。
- **解决方案**：
    1. 引入局部 DOM 操作替代全量 `updateUI` 调用，减少对核心逻辑块的干扰。
    2. 执行重大替换后，必须自检“生命线”逻辑（渲染循环、事件捕获）。
- **教训**：修改 UI 结构时应尽可能实现“UI 与逻辑分离”，严禁在调整样式代码时波及底层的动力学时钟。

### 案例 005：非法的“全量覆盖”事故 (Illegal Full Rewrite)
- **现象**：AI 在尝试修复脚本逻辑时，使用了全量 `write_to_file`，导致用户手动调优的 CSS 细节被瞬间抹除。
- **解决方案**：**绝对禁止**全量重写 UI 文件，必须使用 `ReplacementChunks` 精准修补。

### 案例 006：防御性逻辑的“误杀”故障 (Self-Killing Initialization)
- **现象**：图层缩略图显示为空白。
- **解决方案**：初始化动画时使用 `requestAnimationFrame` 确保 DOM 挂载后再运行。

### 案例 007：自以为是的“折射算法”复发 (Obsessive Refraction Relapse)
- **现象**：AI 强行在 Shader 中加入透镜折射（Refraction）和视差（Parallax）逻辑，导致用户精心设计的几何素材出现扭曲和错位。
- **根源**：AI 过度迷信“物理真实感”，忽视了用户“1:1 几何诚信”的最高设计初衷。
- **解决方案 (最高禁令)**：**禁止**在未得到明确指令的情况下加入任何非线性位移或物理扭曲算法。渲染引擎必须保持“纯粹采样”状态，即：`finalUV = uv`。
