# GrayLight Studio 工业级需求总账 (Comprehensive Master Ledger)

> [!DANGER]
> **最高指令：此文档记录了 USER 自项目启动以来（2026-05-11 全天）提出的所有核心修改。**
> **1. 改什么再动什么：禁止在修改某一功能时波及其他无关条目。**
> **2. 禁止随意合并优化：严禁擅自精简、合并或改写 USER 已确认的详细描述语。**
> **任何一次代码重构、重写、局部修补，都必须确保以下条目 100% 存在。丢失任何细节即视为严重的工程事故。**

---

## 1. 基础物理交互 (Core Physics)

- [x] **1-254 像素保护**：Shader 采样必须执行 `if (h < 0.01 || h > 0.99) discard;` 以彻底移除纯黑/白杂质。
- [x] **3x3 驱动柔化**：严禁直接采样原图作为流光驱动。必须执行 3x3 均值模糊采样，提升质感并消除锯齿。
- [x] **曝光保护 (Tone-Mapping)**：最终输出经过 `1.0 - exp(-effect * 1.5)` 处理，确保高亮处不“爆掉”。
- [x] **底图 1:1 绝对诚信**：当 `u_is_base == 1` 时，严禁任何 UV 扭曲 or 物理偏移。
- [x] **50% 锐度保护**：WebGL 渲染层必须锁定 50% 锐度偏移保护，确保边缘清晰度符合工业基准。

## 2. 图层与资产逻辑 (Assets & Layers)

- [x] **重复导入解锁**：`file-input` 在 `onchange` 后必须立即执行 `e.target.value = ''`，支持同一文件多次载入。
- [x] **首图自动铺满 (Auto-Fit)**：首张素材载入后，视口必须自动计算 Scale（约 85% 填充），严禁默认 1:1 怼脸。
- [x] **底图自动让渡协议**：**[核心]** 当图层数量 = 1 时，该层必须保留光效。当图层数量 > 1 时，底图（索引 0）的 `u_bright` 必须自动设为 0。
- [x] **位置即底图协议 (Location-based Base)**：有且仅有一个底图，列表最下方的图层（索引 0）自动作为底图。被拖动到该位置的图层默认开启可见，但允许手动关闭。
- [x] **物理排序手柄**：图层左侧必须有 `drag-handle`，且支持 PointerCapture 实时平滑物理排序。
- [x] **入位底图强制激活**：图层被拖动至索引 0（列表底部）晋升为底图时，系统必须自动将其 `visible` 设为 `true`，确保地基稳固。
- [x] **底图变更视角自适应**：当底图对象（索引 0）由于排序或载入发生更替时，必须自动触发视口复位（85% 填充），严禁出现因分辨率不匹配导致的“怼脸”现象。
- [x] **载入零延迟**：首张素材载入时必须瞬间显示在计算后的 Scale 位置。同时，侧边栏图层卡片进入时严禁使用 Scale 缩放动画，仅允许轻微位移或直接显示。

## 3. 专业抠图工作流 (Keying Workflow)

- [x] **8倍物理放大镜**：吸色预览区配备独立 Canvas 渲染的像素级放大镜，支持 Crosshair 精准定位。
- [x] **吸色智能回弹**：检测到用户吸色且当前容差为 0 时，系统必须自动将容差设为 **10**，提供即时视觉反馈。
- [x] **实时色值探测 (Real-time Probe)**：鼠标在抠图预览区移动时，Hex 输入框必须实时显示当前指向像素的 16 进制色值。
- [x] **十六进制强制同步**：Hex 输入框必须支持双向同步，且输入后自动执行 `toUpperCase()` 保持格式统一。
- [x] **抠图参数重置 (Keying Reset)**：底部配备红色 `clear-keying-btn` (叉号)，点击瞬间重置容差为 10 并清除 Shader 状态。
- [x] **视口快捷复位 (Quick View Reset)**：在抠图缩略图右上角配备绝对定位的**蓝色旋转图标按钮**，点击执行全局视口复位（回归到 85% Auto-Fit 铺满状态）。

## 4. 极致交互细节 (Pro Interaction)

- [x] **点击即控 (Focus Protocol)**：点击时间轴上的任何数字必须同时触发 **[自动暂停]** 与 **[全量对焦 select()]**。
- [x] **无极滚轮微调**：数字输入框监听 `onwheel`，支持以 0.1 为步长的滚轮调节。
- [x] **AB 联动防撞**：A/B 点之间保持 0.01 最小物理间距。
- [x] **速率上限锁定**：播放速率输入框与滑块最大值严格限制在 **2.0x**，严禁出现非法超大数值。
- [x] **侧边栏常驻开关**：`#sidebar-toggle` 按钮位于容器边缘外，确保折叠状态下依然绝对可见。

## 5. UI 审美与动力学 (Kinetics)

- [x] **物理级缝合 (Seamless Stitching)**：切换按钮必须作为侧边栏的“延伸 (Extension)”而非悬浮。严禁 `border-left`，通过 `margin-left: -1px` 压住主体边框，消除断层感。
- [x] **变色全量同步 (0.5s Sync)**：日夜模式切换时，侧边栏与按钮的 **background/color** 必须与 `body` 统一锁定在 **0.5s** 且 0 延迟。
- [x] **位移动力学 (Kinetic Breath)**：保留 `transform` 的 **0.1s 启动延迟**，维持那种“由于惯性被甩出来”的先行拉扯感。
- [x] **冰川玻璃 (Glacier Glass)**：背景模糊锁定 40px，使用动态 Aurora 背景。主视口严禁使用棋盘格背景。
- [x] **复位图标指向**：视口复位必须使用 **Rotate-CCW (逆时针旋转)** 图标。
- [x] **Logo 绝对物理居中**：主视口 Placeholder 强制绝对居中。
- [x] **缩略图比例诚信 (Aspect Ratio Integrity)**：抠图预览区的 Canvas 必须根据图片原始宽高比动态调整高度，严禁任何形式的拉伸变形。
- [x] **穿透式视口交互 (Kinetic Viewport)**：主 Canvas 必须设置 `pointer-events: none`。所有滚轮缩放与鼠标拖动事件必须由 `viewport` 容器捕获，实现无极平滑变换。
- [x] **渲染地基同步 (Canvas Sync)**：首张素材载入时，Canvas 物理像素宽高必须强制同步为素材原始宽高，严禁使用默认 Canvas 尺寸导致黑屏。
- [x] **缩略图独立交互 (Local Thumbnail Interaction)**：侧边栏缩略图支持私有滚轮缩放与鼠标拖拽位移，专供精准吸色使用，互不干扰主视口。
- [x] **弹性视口回弹 (Elastic Snap-back)**：主视口拖拽具备物理弹性，严禁将图片彻底拖离视野。超出安全边界（150px 缓冲）后自动执行阻尼回弹。
- [x] **格式友好申明**：侧边栏“导入素材”按钮下方必须清晰标注支持 PNG, JPG, WEBP 格式。

## 6. 严禁行为与事故复盘 (Anti-Patterns & Lessons)

- [x] **严禁私加外部资源**：必须坚持“自采样”逻辑，严禁偷渡 `light.png`。
- [x] **严禁全量重写时遗漏核心逻辑**：包括 `layers.push`、`sidebar-toggle`、`sync` 函数等。
- [x] **严禁遮挡切换按钮**：侧边栏容器严禁使用 `overflow-hidden`。
- [x] **严禁交互时全量重绘 (Zero Flicker)**：点击折叠面板、吸色、滑动容差等高频交互严禁调用 `updateUI()` 全量重绘，必须通过局部 DOM 操作（classList, textContent）实现，消除黑屏闪烁。
- [x] **严禁凭记忆修改 (Blind Editing Forbidden)**：**[血泪教训]** 在执行任何删除、重构或全量覆盖前，必须物理执行 `view_file` 核对上下文，严禁凭 AI 记忆进行盲目操作。
- [x] **严禁破坏生命维持系统 (Core System Integrity)**：每一版代码提交前必须自检：播放/暂停、视口拖拽、无极缩放这三大“生命线”逻辑是否依然物理存在且未被误删。
- [x] **严禁选择器虚构 (Selector Naming Integrity)**：在执行局部 DOM 更新（如 `syncLayers`）时，必须物理验证 Classname（如 `.eye-btn` vs `.visible-btn`）的真实性，严禁凭 AI 记忆编写。
- [x] **严禁映射逻辑倒置 (Mapping Inversion Forbidden)**：在 UI 列表（从上到下）与内存数组（从底到顶）同步时，必须严格执行 `DOM[index]` ➔ `layers[length - 1 - index]` 映射，严禁出现镜像错位。
- [x] **严禁自我演绎需求 (No Over-interpretation)**：严格执行 USER 账本原文，回复时严禁加括号进行任何形式的自我发散或逻辑扩充。

## 7. 架构与稳定性 (Architecture)

- [x] **预乘透明度**：纹理载入必须开启 `UNPACK_PREMULTIPLY_ALPHA_WEBGL`，彻底消除 PNG 半透明边缘的黑色脏影。
- [x] **层级序反向映射**：UI 列表“从上到下”的物理顺序必须精准对应 WebGL “从后到前”的渲染深度序。
- [x] **异步初始化安全**：图片 `onload` 必须作为 WebGL 纹理生成的唯一触发源，严禁出现空纹理占用。
- [x] **缩略图物理映射**：缩略图 `drawImage` 必须基于 `layer.img` 的原始物理尺寸进行 `sw/sh` 逆向计算，确保缩放中心不偏移。

---
*记录起始：2026-05-11 01:27*
*最后更新：2026-05-11 20:35*
*版本：V4.5 (Dynamic Sorting & Logic Alignment)*
